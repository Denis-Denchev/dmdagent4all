# DMD Agent 4 All does not store secrets in .env files.
# Use this file only for non-sensitive local development switches.

DMDAGENT_ENV=development
DMDAGENT_CONFIG=~/.local/share/dmdagent4all/config.yaml
