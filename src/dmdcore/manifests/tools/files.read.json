{
  "name": "files.read",
  "description": "Read a non-secret text file inside an allowed workspace root.",
  "risk": 0,
  "permissions": [],
  "args_schema": {
    "path": "absolute path inside allowed roots or path relative to current workspace",
    "path_from_previous_step_match": "inside a multi_tool_plan, choose a file from the previous files.list result by matching this text"
  },
  "approval_required": false,
  "cloud_allowed": false,
  "default_enabled": true,
  "sandbox_required": true
}
